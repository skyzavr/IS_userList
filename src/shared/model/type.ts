export type dataDesc = {
  id: string;
  name: string;
  jobTitle: string;
  department: string;
  company: string;
};

export type IOoptions = {
  root: null;
  rootMargin: string;
  threshold: number;
};
